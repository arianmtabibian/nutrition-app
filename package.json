{
  "name": "nutrition-app",
  "version": "1.0.10",
  "description": "Nutrition tracking app - Fixed meal creation API endpoint",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}
