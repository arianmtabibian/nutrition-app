{
  "name": "nutrition-app",
  "version": "1.0.1",
  "description": "Nutrition tracking app - Fixed registration and onboarding flow",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}
