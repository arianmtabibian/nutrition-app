{
  "name": "nutrition-app",
  "version": "1.0.4",
  "description": "Nutrition tracking app - Fixed meals and favorites API endpoints with enhanced logging",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}
