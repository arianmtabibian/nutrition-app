{
  "name": "nutrition-app",
  "version": "1.0.11",
  "description": "Nutrition tracking app - Fixed post persistence and database schema",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}
