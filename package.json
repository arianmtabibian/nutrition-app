{
  "name": "nutrition-app",
  "version": "1.0.8",
  "description": "Nutrition tracking app - Fixed OpenAI API key error handling",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}
