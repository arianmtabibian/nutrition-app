{
  "name": "nutrition-app",
  "version": "1.0.2",
  "description": "Nutrition tracking app - Fixed onboarding blank screen and TypeScript errors",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}
