{
  "name": "nutrition-app",
  "version": "1.0.5",
  "description": "Nutrition tracking app - Fixed duplicate userId declaration syntax error",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}
